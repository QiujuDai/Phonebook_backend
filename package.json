{
  "name": "Phonebook",
  "version": "1.0.0",
  "description": "Phonebook backend",
  "license": "MIT",
  "author": "charis",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd ../Phonebook_fontend && npm run build && cp -r dist ../Phonebook_backend && cd ../Phonebook_backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "start": "node index.js ",
    "lint": "Eslint ."
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^18.0.1",
    "express": "^5.2.1",
    "mongoose": "^9.10.1",
    "morgan": "^1.12.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@stylistic/eslint-plugin": "^5.10.0",
    "eslint": "^10.11.0",
    "globals": "^17.12.0"
  }
}
